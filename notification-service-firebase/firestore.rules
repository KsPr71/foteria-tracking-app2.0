rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Solo las Cloud Functions (admin) escriben; la app no accede directo a Firestore
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
